[pytest]
# Run from the backend/ directory: `./.venv/bin/pytest` (or `make test`).
# pythonpath=. makes the `app` package importable without installation.
pythonpath = .
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
addopts = -q
