{
  "name": "nda-review-word-addin",
  "version": "1.0.0",
  "private": true,
  "description": "NDA Review Word add-in (taskpane). Pure redline/diff helpers are unit-tested with Node's built-in test runner — no dependencies.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.4"
  }
}
